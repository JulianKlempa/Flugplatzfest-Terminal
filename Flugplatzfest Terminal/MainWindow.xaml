<Window x:Class="Flugplatzfest_Terminal.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModels="clr-namespace:Flugplatzfest_Terminal.ViewModels"
        xmlns:views="clr-namespace:Flugplatzfest_Terminal.Views"
        mc:Ignorable="d"
        Title="Terminal" Height="450" Width="800" WindowState="Maximized">
    <Grid>
        <Grid.Resources>
            <DataTemplate DataType="{x:Type viewModels:TerminalViewModel}">
                <views:Terminal />
            </DataTemplate>
            <DataTemplate DataType="{x:Type viewModels:SettingsViewModel}">
                <views:Settings />
            </DataTemplate>
        </Grid.Resources>
        <ContentControl Content="{Binding CurrentViewModel}" />
    </Grid>
</Window>